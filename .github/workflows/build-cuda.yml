name: Build llama.cpp forks with CUDA

# Best-of-all multi-fork CUDA builder.
# Builds 4 llama.cpp forks for CUDA 13.2 (RTX 5090 / sm_120 + all prior gens), amd64 only:
#   - vanilla      : ggml-org/llama.cpp
#   - turboquant   : TheTom/llama-cpp-turboquant
#   - atomic       : AtomicBot-ai/atomic-llama-cpp-turboquant
#   - prism        : PrismML-Eng/llama.cpp
# For EVERY fork we detect TWO independent sources and emit up to two release builds:
#   - stable : latest GitHub release of the fork (prerelease releases are treated as
#              releases too — we only need the source commit, never the release binaries).
#   - nightly: HEAD commit of the fork's tracked branch.
# Binaries land in separate subdirs so they never collide: cuda-13.2/<subdir>/llama-server.
# Source of truth for what to build: forks.json (committed in repo).
on:
  schedule:
    # Daily at 00:00 UTC — catches new releases and branch moves.
    - cron: '0 0 * * *'
  workflow_dispatch:
    inputs:
      force_build:
        description: 'Force build all forks (stable+nightly) even if unchanged'
        required: false
        type: boolean
        default: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  check-forks:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.plan.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      # Restore last-built refs from Actions cache (one key per fork+mode).
      # Cache TTL is set in repo Settings > Actions > General > Cache
      # (public repos cap at 7 days; self-hosted/paid can raise it).
      - name: Restore last-built refs (cache)
        id: restore-refs
        uses: actions/cache@v6
        with:
          path: .cache-refs
          key: fork-refs-v2-${{ github.run_id }}
          restore-keys: |
            fork-refs-v2-

      - name: Plan which forks changed
        id: plan
        run: |
          FORCE="${{ github.event.inputs.force_build }}"

          COUNT=$(jq '.forks | length' forks.json)
          ARCHS=$(jq -r '.architectures' forks.json)
          MATRIX_ITEMS="[]"

          for i in $(seq 0 $((COUNT-1))); do
            NAME=$(jq -r ".forks[$i].name" forks.json)
            REPO=$(jq -r ".forks[$i].repo" forks.json)
            BRANCH=$(jq -r ".forks[$i].branch // \"\"" forks.json)
            SUBDIR=$(jq -r ".forks[$i].subdir" forks.json)

            # ---- STABLE: latest release (prerelease releases count as releases) ----
            REL=$(curl -s "https://api.github.com/repos/${REPO}/releases" | jq -r '.[0] // empty')
            if [ -n "$REL" ]; then
              REL_TAG=$(echo "$REL" | jq -r '.tag_name')
              REL_PRE=$(echo "$REL" | jq -r '.prerelease')
              REL_REF=$(echo "$REL" | jq -r '.target_commitish')
              # target_commitish may be a branch name (e.g. turboquant) -> resolve to SHA
              if ! echo "$REL_REF" | grep -qE '^[0-9a-f]{40}$'; then
                REL_REF=$(curl -s "https://api.github.com/repos/${REPO}/commits/${REL_REF}" | jq -r '.sha')
              fi
              STABLE_REF="$REL_REF"
              STABLE_TAG="$REL_TAG"
              STABLE_PRE="$REL_PRE"
            else
              STABLE_REF=""
              STABLE_TAG=""
              STABLE_PRE="false"
            fi

            # ---- NIGHTLY: HEAD of tracked branch (only if a branch is configured) ----
            if [ -n "$BRANCH" ]; then
              NIGHTLY_REF=$(curl -s "https://api.github.com/repos/${REPO}/commits/${BRANCH}" | jq -r '.sha')
            else
              NIGHTLY_REF=""
            fi

            # ---- Emit matrix items, comparing against last-built refs ----
            LAST_STABLE=$(cat ".cache-refs/${NAME}-stable.txt" 2>/dev/null || echo "")
            LAST_NIGHTLY=$(cat ".cache-refs/${NAME}-nightly.txt" 2>/dev/null || echo "")

            if [ -n "$STABLE_REF" ] && { [ "$FORCE" = "true" ] || [ "$STABLE_REF" != "$LAST_STABLE" ]; }; then
              echo "Fork $NAME: stable ref=$STABLE_REF (tag=$STABLE_TAG, prerelease=$STABLE_PRE) -> changed"
              ITEM=$(jq -n --arg name "$NAME" --arg repo "$REPO" --arg branch "$BRANCH" \
                      --arg mode "stable" --arg ref "$STABLE_REF" --arg reltag "$STABLE_TAG" \
                      --arg pre "$STABLE_PRE" --arg subdir "$SUBDIR" --arg arch "$ARCHS" \
                      '{name:$name, repo:$repo, branch:$branch, mode:$mode, ref:$ref, reltag:$reltag, prerelease:$pre, subdir:$subdir, architectures:$arch}')
              MATRIX_ITEMS=$(echo "$MATRIX_ITEMS" | jq --argjson it "$ITEM" '. + [$it]')
            else
              echo "Fork $NAME: stable ref=$STABLE_REF unchanged (last=$LAST_STABLE)"
            fi

            if [ -n "$NIGHTLY_REF" ] && { [ "$FORCE" = "true" ] || [ "$NIGHTLY_REF" != "$LAST_NIGHTLY" ]; }; then
              echo "Fork $NAME: nightly ref=$NIGHTLY_REF -> changed"
              ITEM=$(jq -n --arg name "$NAME" --arg repo "$REPO" --arg branch "$BRANCH" \
                      --arg mode "nightly" --arg ref "$NIGHTLY_REF" --arg reltag "" \
                      --arg pre "true" --arg subdir "$SUBDIR" --arg arch "$ARCHS" \
                      '{name:$name, repo:$repo, branch:$branch, mode:$mode, ref:$ref, reltag:$reltag, prerelease:$pre, subdir:$subdir, architectures:$arch}')
              MATRIX_ITEMS=$(echo "$MATRIX_ITEMS" | jq --argjson it "$ITEM" '. + [$it]')
            else
              echo "Fork $NAME: nightly ref=$NIGHTLY_REF unchanged (last=$LAST_NIGHTLY)"
            fi
          done

          echo "matrix=$(echo "$MATRIX_ITEMS" | jq -c '{include: .}')" >> $GITHUB_OUTPUT

  build:
    needs: check-forks
    if: needs.check-forks.outputs.matrix != '{"include":[]}'
    runs-on: ubuntu-latest   # amd64 only (RTX 5090 host)
    permissions:
      contents: write   # needed to publish per-fork releases via gh
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.check-forks.outputs.matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      # Skip the build entirely if a release with this exact fork+sha tag
      # already exists — no point burning 40-60 min rebuilding identical binaries.
      - name: Check if release already exists
        id: chk
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          FORCE="${{ github.event.inputs.force_build }}"
          SHA12="${{ matrix.ref }}"
          SHA12="${SHA12:0:12}"
          if [ "${{ matrix.mode }}" = "stable" ]; then
            TAG="${{ matrix.name }}-${SHA12}"
          else
            TAG="${{ matrix.name }}-nightly-${SHA12}"
          fi
          echo "Checking for existing release: $TAG (force_build=$FORCE)"
          if gh release view "$TAG" --repo "${{ github.repository }}" >/dev/null 2>&1; then
            if [ "$FORCE" = "true" ]; then
              # Force rebuild: drop the existing release AND its tag so the
              # later `gh release create` can reuse the same tag.
              echo "force_build: deleting existing $TAG to rebuild"
              gh release delete "$TAG" --repo "${{ github.repository }}" --yes >/dev/null 2>&1 || true
              gh api -X DELETE "/repos/${{ github.repository }}/git/refs/tags/${TAG}" >/dev/null 2>&1 || true
              echo "skip=false" >> "$GITHUB_OUTPUT"
            else
              echo "Release $TAG already exists — skipping build."
              echo "skip=true" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Maximize build space
        run: |
          echo "=== Initial Disk Space ==="
          df -h
          sudo apt-get remove -y '^aspnetcore-.*' '^dotnet-.*' '^llvm-.*' 'php.*' '^mongodb-.*' '^mysql-.*' \
            azure-cli google-chrome-stable firefox powershell mono-devel libgl1-mesa-dri --fix-missing || true
          sudo apt-get autoremove -y || true
          sudo apt-get clean || true
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL \
            /usr/local/share/boost /usr/share/swift /usr/local/.ghcup "$AGENT_TOOLSDIRECTORY"
          docker system prune -af || true
          echo "=== After Cleanup ==="
          df -h

      - name: Setup Compiler Cache (ccache)
        uses: actions/cache@v6
        with:
          path: .ccache
          key: ${{ runner.os }}-ccache-cuda13.2-${{ matrix.name }}-${{ matrix.mode }}-${{ matrix.ref }}
          restore-keys: |
            ${{ runner.os }}-ccache-cuda13.2-${{ matrix.name }}-${{ matrix.mode }}-

      - name: Build ${{ matrix.name }} (${{ matrix.mode }}) with CUDA
        if: steps.chk.outputs.skip != 'true'
        env:
          CUDA_TAG: '13.2.0-cudnn-devel-ubuntu24.04'
          ARCHS: ${{ matrix.architectures }}
          SUBDIR: ${{ matrix.subdir }}
          REPO: ${{ matrix.repo }}
          BRANCH: ${{ matrix.branch }}
          REF: ${{ matrix.ref }}
          RELTAG: ${{ matrix.reltag }}
          MODE: ${{ matrix.mode }}
        run: |
          mkdir -p $PWD/.ccache
          docker pull nvidia/cuda:${CUDA_TAG}
          docker run --rm \
            -v $PWD:/workspace \
            -v $PWD/.ccache:/root/.ccache \
            -e CCACHE_DIR=/root/.ccache \
            -e REPO="${REPO}" \
            -e BRANCH="${BRANCH}" \
            -e REF="${REF}" \
            -e RELTAG="${RELTAG}" \
            -e MODE="${MODE}" \
            -e SUBDIR="${SUBDIR}" \
            -e ARCHS="${ARCHS}" \
            nvidia/cuda:${CUDA_TAG} \
            bash /workspace/scripts/build-inside.sh

          docker rmi nvidia/cuda:${CUDA_TAG} || true
          sudo chown -R $(id -u):$(id -g) $PWD
          df -h

      - name: Create tarball
        if: steps.chk.outputs.skip != 'true'
        run: |
          cd binaries
          tar -czf llama.cpp-cuda-13.2-${{ matrix.name }}-${{ matrix.mode }}-amd64.tar.gz ${{ matrix.subdir }}
          ls -lh *.tar.gz

      - name: Upload artifact
        if: steps.chk.outputs.skip != 'true'
        uses: actions/upload-artifact@v7
        with:
          name: llama.cpp-cuda-13.2-${{ matrix.name }}-${{ matrix.mode }}-amd64
          path: binaries/*.tar.gz
          retention-days: 1

      - name: Write built ref
        run: |
          mkdir -p .cache-refs
          echo "${{ matrix.ref }}" > ".cache-refs/${{ matrix.name }}-${{ matrix.mode }}.txt"

      # Save all fork refs; check-forks restores via restore-keys "fork-refs-v2-".
      - name: Save built refs to cache
        uses: actions/cache@v6
        with:
          path: .cache-refs
          key: fork-refs-v2-${{ github.run_id }}-${{ matrix.name }}-${{ matrix.mode }}
          restore-keys: |
            fork-refs-v2-
          save-always: true

      # Publish THIS fork's release immediately on success — independent of other
      # matrix jobs, so a failure elsewhere doesn't block or skip this publish.
      - name: Publish per-fork release
        if: success() && steps.chk.outputs.skip != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          name="${{ matrix.name }}"
          mode="${{ matrix.mode }}"
          sha="${{ matrix.ref }}"
          sha12="${sha:0:12}"
          # Explicit tarball path (do NOT glob binaries/*.tar.gz — the CUDA
          # asset would sort before the fork tarball and break parsing).
          f="binaries/llama.cpp-cuda-13.2-${name}-${mode}-amd64.tar.gz"
          if [ ! -f "$f" ]; then
            echo "FATAL: built tarball missing: $f"
            exit 1
          fi
          if [ "$mode" = "stable" ]; then
            tag="${name}-${sha12}"
            prerelease="false"
          else
            tag="${name}-nightly-${sha12}"
            prerelease="true"
          fi
          # Release notes. The vanilla release also ships a shared CUDA runtime
          # asset (handled below) for local, non-Docker use.
          if [ "$name" = "vanilla" ]; then
            notes_args=(--notes "CUDA 13.2 (RTX 5090 / sm_120) build of **$name** ($mode).\n\n**Local (non-Docker) use:** this release also ships \`cuda-runtime-13.2-amd64.tar.gz\` — download it once and place its \`.so\` files next to any fork's binaries. The llama-swap Docker image does NOT need it.")
          else
            notes_args=(--notes "CUDA 13.2 (RTX 5090 / sm_120) build of **$name** ($mode).")
          fi
          # The vanilla release also carries the shared CUDA runtime asset
          # (built into cuda-asset/, outside binaries/) so local users on a
          # bare OS can grab the .so's once. Other forks do not bundle it; the
          # swap image gets CUDA from its nvidia/cuda:runtime base instead.
          extra_args=()
          if [ "$name" = "vanilla" ] && ls cuda-asset/cuda-runtime-*.tar.gz >/dev/null 2>&1; then
            cuda_asset=$(ls cuda-asset/cuda-runtime-*.tar.gz | head -1)
            extra_args+=("$cuda_asset")
            echo "  + CUDA runtime asset: $cuda_asset"
          fi
          echo "Publishing $tag (prerelease=$prerelease) from $f"
          gh release create "$tag" "$f" "${extra_args[@]}" \
            --repo "${{ github.repository }}" \
            --title "$tag" \
            "${notes_args[@]}" \
            $( [ "$prerelease" = "true" ] && echo "--prerelease" )

  # Trigger the swap-image build ONLY after ALL parallel builds finish.
  # if: always() so it runs even if some builds failed; guarded below so it
  # only fires when at least one release was actually published today.
  trigger-swap:
    needs: [build, check-forks]
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Trigger image repo build
        env:
          GH_TOKEN: ${{ secrets.IMAGE_REPO_TOKEN }}
          MATRIX_JSON: ${{ needs.check-forks.outputs.matrix }}
        run: |
          if [ -z "${GH_TOKEN:-}" ]; then
            echo "::error::IMAGE_REPO_TOKEN secret is not set — image auto-build cannot be triggered"
            echo "::error::Create a GitHub PAT with 'workflow' scope and add it as IMAGE_REPO_TOKEN secret"
            exit 1
          fi
          TODAY=$(date -u +%Y-%m-%d)
          # Count releases published TODAY via JSON (reliable column parsing).
          COUNT=$(gh release list --repo "${{ github.repository }}" --limit 100 \
            --json publishedAt \
            | jq --arg d "$TODAY" '[.[] | select(.publishedAt | startswith($d))] | length')
          if [ "${COUNT:-0}" -eq 0 ]; then
            echo "No successful releases today — skipping image repo trigger."
            exit 0
          fi
          # Pick mode: nightly if any matrix item is nightly, else stable.
          if echo "$MATRIX_JSON" | jq -e '.include | any(.mode == "nightly")' >/dev/null 2>&1; then
            IMG_MODE=nightly
          else
            IMG_MODE=stable
          fi
          echo "Triggering image repo build (mode=$IMG_MODE, $COUNT releases available)"
          if ! gh workflow run build-image.yml \
            --repo tayuLuc/llama-swap-cuda-images \
            -f mode="$IMG_MODE"; then
            echo "::error::IMAGE_REPO_TOKEN missing, expired, or insufficient — image auto-build not triggered"
            exit 1
          fi
